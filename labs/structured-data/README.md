# Playing with structured data


- Define a `User` struct containing the fields:
  * name,
  * email, 
  * uri,
  * active,

- Create a new user from there

- Implement a `new` function to this struct
- Implement a `deactivate` function to this struct
